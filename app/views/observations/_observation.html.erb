<tr>
  <td><%= observation.obs_date %></td>
  <td><%= observation.observation_type_names %></td>
  <td><%= truncate(observation.comment) %></td>
  <td><%= observation.equipment_names %></td>
  <td><%= observation.materials_with_rates %></td>
  <td>
    <%= link_to 'Show', observation_path(observation)%>
    <%= link_to 'Edit', edit_observation_path(observation) if current_user %>
  </td>
</tr>