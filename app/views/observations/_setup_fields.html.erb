<%= f.inputs do %>
  <%= f.input :equipment, :collection=>Equipment.by_company(current_user.company).ordered, :include_blank => false %>
  <%= link_to_remove_fields 'Remove', f%>

  <div class='material-transaction'>
    <%= f.inputs :for => :material_transactions do |builder| %>
      <%= render 'material_transaction_fields', :f => builder %>
    <% end %>
  </div>
  <%= link_to_add_fields 'Add Material', f, :material_transactions %>
<% end %>
