<!DOCTYPE html>
<html>
<head>
  <title>Aglog : <%= controller.controller_name.titleize %> : <%= controller.action_name.titleize %></title>
  <%= stylesheet_link_tag "application" %>
  <%= javascript_include_tag "application" %>

  <%= csrf_meta_tag %>
</head>
<body>

  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <a class="brand" href="#">Aglog</a>
        <div class="nav-collapse">
          <ul class="nav">
            <li><%= link_to 'Observations', observations_path %></li>
            <li><%= link_to 'People', people_path, :accesskey => 'P' %></li>
            <li><%= link_to 'Material', materials_path, :accesskey =>'M' %></li>
            <li><%= link_to 'Equipment', equipment_index_path, :accesskey => 'E' %></li>
            <li><%= link_to 'Units', units_path, :accesskey => 'U' %></li>
            <li><%= link_to 'Areas', areas_path, :accesskey => 'A' %></li>
            <% if user_signed_in? %>
              <li><%= link_to 'Sign Out', destroy_user_session_path %></li>
            <% else %>
              <li><%= link_to 'Sign In', new_user_session_path  %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>

  <%= yield :header %>
</div>
<div id="main_page">

  <p style="color: green"><%= flash[:notice] %></p>
  <p style="color: red"><%= flash[:error] %></p>

  <%= yield %>

</div>
</body>
</html>
