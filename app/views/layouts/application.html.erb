<!DOCTYPE html>
<html>
<head>
  <title>Aglog : <%= controller.controller_name.titleize %> : <%= controller.action_name.titleize %></title>
  <link href="//netdna.bootstrapcdn.com/bootstrap/3.1.0/css/bootstrap.min.css" rel="stylesheet">
  <script src="//netdna.bootstrapcdn.com/bootstrap/3.1.0/js/bootstrap.min.js"></script>
  <%= stylesheet_link_tag "application" %>
  <%= javascript_include_tag "application" %>

  <%= csrf_meta_tag %>
</head>
<body>

  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <a class="brand" href="#">Aglog</a>
        <div class="nav-collapse">
          <ul class="nav">
            <li <%= "class=active" if controller.controller_name == 'observations' %>><%= link_to 'Observations', observations_path %></li>
            <li <%= "class=active" if controller.controller_name == 'people' %>><%= link_to 'People', people_path, :accesskey => 'P' %></li>
            <li <%= "class=active" if controller.controller_name == 'materials'%>><%= link_to 'Material', materials_path, :accesskey =>'M' %></li>
            <li <%= "class=active" if controller.controller_name == 'equipment' %>><%= link_to 'Equipment', equipment_index_path, :accesskey => 'E' %></li>
            <li <%= "class=active" if controller.controller_name == 'units' %>><%= link_to 'Units', units_path, :accesskey => 'U' %></li>
            <li <%= "class=active" if controller.controller_name == 'areas' %>><%= link_to 'Areas', areas_path, :accesskey => 'A' %></li>
            <% if user_signed_in? %>
              <li><%= link_to 'Sign Out', destroy_user_session_path, :method => :delete %></li>
            <% else %>
              <li><%= link_to 'Sign In', new_user_session_path  %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>

  <%= yield :header %>
</div>
<div id="main_page">

  <p style="color: green"><%= flash[:notice] %></p>
  <p style="color: red"><%= flash[:error] %></p>

  <%= yield %>

</div>
</body>
</html>
